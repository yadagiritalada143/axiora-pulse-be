"""
app/core/config.py
────────────────────────────────────────────────────────────────────────────────
DEPRECATED: This file is no longer used.
All configuration is now read directly from the .env file via os.getenv() and
python-dotenv in each module that needs it.

This file is kept as an empty stub to avoid breaking any stale bytecode caches.
It will be deleted in the next cleanup pass.
"""
